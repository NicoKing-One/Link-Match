# 金币兑换页面资源

页面入口：金币兑换 / 资源兑换入口。

本文件夹只放金币兑换页面需要调用的图片和图标。接入时页面可以统一从：

```text
src/assets/UI-ICON/exchange-page/
```

读取资源。

## 按钮

- `button-back-icon.png`：顶部返回按钮。
- `button-return-home-bg.png`：底部“返回首页”按钮背景图，文字建议用 HTML/CSS 覆盖。
- `button-exchange-bg.png`：单个“兑换”按钮背景图，文字建议用 HTML/CSS 覆盖。

## 全屏背景

- `page-bg.png`：页面全屏背景图。三个二级页当前统一使用这一张背景。

## 顶部标题

- `title-bg-coin-exchange.png`：顶部“金币兑换”标题背景图，文字建议用 HTML/CSS 覆盖。

## 主体模块

- `module-exchange-coin-bg.png`：顶部金币资源模块背景。
- `coin-exchange-resource-bg.png`：金币资源栏背景。
- `module-exchange-item-bg.png`：单个兑换项模块背景。

## 图标

- `icon-coin.png`：金币图标。
- `icon-stamina.png`：体力图标。
- `icon-hint.png`：提示图标。
- `icon-shuffle.png`：洗牌图标。
